# setup

- install uv

    ```
    curl -LsSf https://astral.sh/uv/install.sh | sh
    ```

- install a tool 
    
    ```
    uv tool install git+https://github.com/omardrwch/setup/uvtools/<tool-dir>
    # or
    cd <tool-dir>
    uv tool install .
    ```

